<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/nav_header_height"
    android:background="#1c3b4e"
    android:gravity="bottom"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">


    <ImageView
        android:id="@+id/am_nav_header_profile"
        style="@style/NavHeaderProfile"
        android:layout_width="60dp"
        android:layout_height="60dp"
        app:srcCompat="@drawable/ic_profile" />


    <TextView
        android:id="@+id/am_nav_header_Title"
        style="@style/NavHeaderTitle"
        android:layout_below="@+id/am_nav_header_profile" />

    <TextView
        android:id="@+id/am_nav_header_tvEmail"
        style="@style/NavHeaderEmail"
        android:layout_below="@+id/am_nav_header_Title" />


    <ImageView
        android:id="@+id/am_nav_header_netCheckImv"
        style="@style/NavHeaderNetCheck"
        android:layout_width="15dp"
        android:layout_height="15dp" />

</RelativeLayout>
